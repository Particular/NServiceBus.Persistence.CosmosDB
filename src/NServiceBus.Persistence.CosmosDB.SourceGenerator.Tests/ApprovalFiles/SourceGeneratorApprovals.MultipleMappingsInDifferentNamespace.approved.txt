// <auto-generated/>
using System;
using Microsoft.Azure.Cosmos;
using NServiceBus.Persistence.CosmosDB;
using Some.Other.Namespace;
using Yet.Another.Namespace;
using System.Collections.Generic;
using NServiceBus;

namespace Some.Complex
{
    internal partial class PartitionKeyMapper
    {

        protected override bool TryMapMessageCore(object message, out PartitionKey? partitionKey, out ContainerInformation? containerInformation)
        {
            partitionKey = null;
            containerInformation = null;
            if(message is IProvideOrderId)
            {
                var (invoker, container, state) = Extractors[typeof(IProvideOrderId)];
                partitionKey = invoker(message, state);
                containerInformation = container;
                return true;
            }
            if(message is OrderDeclined)
            {
                var (invoker, container, state) = Extractors[typeof(OrderDeclined)];
                partitionKey = invoker(message, state);
                containerInformation = container;
                return true;
            }
            return false;
        }

    }
}